# 配置npm镜像源为淘宝源
registry=https://registry.npmmirror.com/

# 安装包时保存精确版本号（避免自动升级到新版本）
save-exact=true

# 严格检查Node.js和npm版本是否符合项目要求
engine-strict=true

# 禁止使用不稳定的版本（如beta、alpha、rc等）
prefer-stable=true

# 禁止使用^、~等版本范围前缀，强制使用精确版本
save-prefix=""

# 使用bun
package-lock=false

# 强制使用精确的版本号，不允许使用范围
save-exact=true

# 安装时忽略package-lock.json（因为使用bun）
package-lock=false

# 禁止自动安装peer dependencies
auto-install-peers=false

# 严格模式，任何版本冲突都会报错
strict-peer-dependencies=true